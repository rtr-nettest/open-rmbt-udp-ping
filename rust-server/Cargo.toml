[package]
name = "udp_server"
version = "1.1.0"
edition = "2021"

[dependencies]
socket2 = "0.5"
hmac = "0.12"
sha2 = "0.10"
clap = "4"
hex = "0.4"
log = { version = "0.4", features = ["std"] }

[target.'cfg(unix)'.dependencies]
signal-hook = "0.3"
